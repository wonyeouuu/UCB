import Vue from 'vue'
import Vuex from 'vuex'
import VueRouter from 'vue-router'

Vue.use(Vuex)
Vue.use(VueRouter)

const state = {
}

const mutations = {
}

export default new Vuex.Store({
    state,
    mutations
})
